import { Typography } from 'antd';

const NotFoundPage = () => (
  <div>
    <Typography.Title level={4}>페이지를 찾을 수 없습니다.</Typography.Title>
    <Typography.Text>올바른 경로를 입력해주세요.</Typography.Text>
  </div>
);

export default NotFoundPage;
